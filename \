#pragma once 
#include "Renderer/RenderContext.h"

class GLFWwindow;

namespace Echo
{
    namespace Echo
    {
        class OpenGLContext : RenderContext
        {
            public: 
                OpenGLContext(GLFWwindow* windowHandle);
                void Init() override;
                void SwapBuffers() override;
            private:
                GLFWwindow* m_WindowHandle;
        };
    }
}
